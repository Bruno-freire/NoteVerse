import Header from '../../components/header'
import Section from '../../components/section'

const HomeScreen = () => (
  <>
    <Header/>
    <main style={{minHeight: '100vh',width: '100%', backgroundColor: '#694ED8', display: 'flex', justifyContent: 'center', alignItems: 'center'}}>
    <Section/>
    </main>
  </>
)

export default HomeScreen